server:
  port: 8300

spring:
  application:
    name: gatway
  profiles:
    active: dev
  #配置中心
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: ${spring.profiles.active}





