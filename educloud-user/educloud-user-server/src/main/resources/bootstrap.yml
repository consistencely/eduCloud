server:
  port: 8002

spring:
  application:
    name: user
  profiles:
    active: dev
  #配置中心
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: ${spring.profiles.active}

